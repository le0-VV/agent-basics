# agent-basics Agent Patterns

- `setup-macos.sh` is intended to be the complete one-command setup surface for this project, so generated markdown templates should remain embedded in the script.
- Keep MemoryHub project source files under `.agents/memoryhub/`; the central MemoryHub installation owns runtime state, database files, embeddings, and MCP/API services.

